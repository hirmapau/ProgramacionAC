#include<stdio.h>
int main() {
	int index = 1;
		while (index <= 5) { //ill keep looping through the code WHILE index is less or equal to 5
			printf("%i\n", index);
			index++;
		}
		return 0;
}